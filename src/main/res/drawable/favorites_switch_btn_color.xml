<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Зеленая иконка, если кнопка нажата -->
    <item android:state_selected="true" android:drawable="@drawable/favorites_btn"/>

    <!-- Зеленая иконка, если кнопка нажата (альтернативно) -->
    <item android:state_pressed="true" android:drawable="@drawable/favorites_full_btn"/>

    <!-- Обычная (пустая) иконка -->
    <item android:drawable="@drawable/favorites_btn"/>

</selector>
